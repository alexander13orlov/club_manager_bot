project/
│   main.py
│   config.py
│   settings_loader.py
│   settings.json
│   README.md
│
├── core/
│   ├── models/
│   │     poll.py
│   │     user.py
│   │     attendance.py
n│   │     schedule.py
│   │     subscription.py
│   │     __init__.py
│   │
│   ├── services/
│   │     poll_service.py
│   │     edit_poll_service.py
│   │     history_service.py
│   │     user_service.py
│   │     attendance_service.py
│   │     schedule_service.py
│   │     subscription_service.py
│   │     __init__.py
│   │
│   ├── repositories/
│   │     history_repo.py
│   │     user_repo.py
│   │     schedule_repo.py
│   │     subscription_repo.py
│   │     __init__.py
│   │
│   └── utils/
│         time_utils.py
│         formatting.py
│         __init__.py
│
├── telegram/
│   ├── bot.py
│   ├── routers.py
│   ├── middlewares/
│   │     __init__.py
│   │     throttling.py
│   │
│   ├── handlers/
│   │     polls.py
│   │     edit_poll.py
│   │     stat.py
│   │     attendance.py
│   │     subscriptions.py
│   │     users.py
│   │     misc.py
│   │     __init__.py
│   │
│   ├── keyboards/
│   │     poll_keyboards.py
│   │     edit_keyboards.py
│   │     inline_builders.py
│   │     __init__.py
│   │
│   └── tasks/
│         autopoll.py
│         poll_updater.py
│         notifications.py
│         __init__.py
│
└── extras/
      export_import.py
      weather.py
      news.py
      stats_export.py
      __init__.py




Разбить код на модули:
0.Главный модуль.
1.Модуль с функциями для генерации и отслеживания опросов в групповом чате.
2.Модуль с функциями для сохранения истории посещения тренировок пользователями.
3.Модуль с функциями работающими с созданием расписания и отслеживания изменения в расписании тренировк.
4.Модлуь с функциями работающими с абонементами. Создание абонементов, настройка абонементов, ведение базы абонементов, различные оповещения, связанные с абонементами
5.Модуль с функциями работающими с регистрацией и хранением данных пользователей.
6.Модуль с дополнительными функциями, вызываемыми по команде пользователя. Дополнительными функциями могут быть: экспорт/импорт настроек и истории опросов, выгрузка статистики, ежедневная публикация новостей, погоды и т.д.
2



Основная цель разработчика: написать приложение, которое будет удобно составлять и динамически обновлять список пользователей, которые хотят участвовать в каком либо событии, указанном в первой строке сообщения. Основная задача бота - отправлять в чат определенное сообщение, которое становится "активным" определенный промежуток времени, во время действия его "активности" пользователи могут выражать свое отношение, бот должден следить за чатом: если пользователи пишут в чат "+" это означает что они хотят участвовать в этом событии, тогда бот редактирует свое сообщение следующим образом: под основным вопросом он пишет порядковый номер, username и имя пользователя. Если пользователь пишет в чат "-", а это сообщение все еще активно, то бот удаляет его из списка. Когда заканчивается время "активности" сообщения, бот перестает реагировать на эти символы (+ и -). Разных сообщений, отправляемых ботом может быть несколько, их уникальные названия и параметры расположены в словаре "commands". На одном уровне с commands могут быть расположены любые другие сущности, используемые для хранения каких либо настроек бота.



Технические элементы реализации:
Напиши на питоне код телеграм бота с использованием асинхронных библиотек.
Токен бота и админский пароль (админский пароль в коде пока использовать не надо, это задел на  будущий функционал) хранятся в отдельном файле .env, который не будет отправляться в гитхаб. Подключение через config.py примерно следующего содержания:
"import os
from dotenv import load_dotenv
# Загружаем .env файл
load_dotenv()
BOT_TOKEN = os.getenv("BOT_TOKEN")  # Берём токен из .env
PASSWORD = os.getenv("PASSWORD")    # Берём пароль из .env"


main.py и settings.json лежат внутри директории bot, main.py запускается командой python -m bot.main.py
Общие принципы работы и описание файла settings.json:
Бот работает только в одном групповом чате, поделенных на темы (топики), id чата указано в настройках settings.json. 
В каждой теме в общем случае свой набор команд, на которые реагирует бот. Бот настраивается через json файл, внутри которого предусмотрено деление на темы: основная тем "root"  и любые другие, при наличии.
Пример содержания settings.json файла. После знака решетка написан комментарий, описывающий данный тег. В данном примере две команды: saber и rapier. Отличаются днями недели запуска по расписанию: команда saber должна запускаться ботом во вторник и четверг, а rapier в пн и пт. Это все указано в settings.json. Для обозначения дней недели использовать следующие слова: mon, tue, wed, thu, fri, sat, sun.

{
    "chats": {
        "-1002665411575": {   #id чата в котором может работать бот
            "admins": ["84324980"], #id админа, который может делать настройки бота (пока не реализовывать, это задел на будущий функционал)
            "topics": { #наименования топиков
                "root": { #основной топик
                    "commands": { # в этом разделе наименования команд, на которые должен реагировать бот
                        "saber": { # уникальное наименование команды. Одновременно с этим является набором символов для ручного запуска этой команды. Команду можно запустить вручную (/saber), также команда может запускаться ботом по расписанию
							"autopoll": "true", # флаг, показывающий будет ли запущена команда по расписанию во время createmsg
                            "autopollsettings": { #настройки для сообщения, которое было создано ботом по расписанию schedule_autopoll
                                "pin": "true", # закрепить сообщение, если pin=true
                                "unpin": "true" # открепить сообщение в время указанное в deactivatemsg, если unpin=true
								"schedule_autopoll": [{ #расписание автоматического запуска команды
                                    "day": "tue", #день недели, в который бот запускает команду, если autopoll=true
                                    "createmsg": "09:00:00", #время, в которое бот запускает команду, если autopoll=true
                                    "deactivatemsg": "21:00:00" #время, до которого бот будет отслеживать ответы пользователей на сообщение, которое генерируется данной командой. Также после этого времени бот должен открепить сообщение, если "unpin"="true"
									}, {
                                    "day": "thu",
                                    "createmsg": "09:00:00",
                                    "deactivatemsg": "21:00:00"
									}
								],
                            "manualpollsettings": { #настройки команды, если пользователь запустил ее вручную.
                                "pin": "true", # закрепить сообщение, если pin=true
                                "unpin": "true", # открепить сообщение в время указанное в deactivatemsg, если unpin=true
								"timetolife": 8 # количество часов, которые бот будет отслеживать ответы пользователей на сообщение, которое генерируется данной командой. Также после этого времени бот должен открепить сообщение, если "unpin"="true"
                            },
                            "question": "Кто сегодня на тренировку по сабле?", #Основной текст сообщения, которое генерирует бот по данной команде.
                        },
                        "rapier": { # уникальное наименование команды. Одновременно с этим является набором символов для ручного запуска этой команды. Команду можно запустить вручную (/rapier), также команда может запускаться ботом по расписанию
							"autopoll": "true", # флаг, показывающий будет ли запущена команда по расписанию во время createmsg
                            "autopollsettings": { #настройки для сообщения, которое было создано ботом по расписанию schedule_autopoll
                                "pin": "true", # закрепить сообщение, если pin=true
                                "unpin": "true" # открепить сообщение в время указанное в deactivatemsg, если unpin=true
								"schedule_autopoll": [{ #расписание автоматического запуска команды
                                    "day": "mon", #день недели, в который бот запускает команду, если autopoll=true
                                    "createmsg": "09:00:00", #время, в которое бот запускает команду, если autopoll=true
                                    "deactivatemsg": "21:00:00" #время, до которого бот будет отслеживать ответы пользователей на сообщение, которое генерируется данной командой. Также после этого времени бот должен открепить сообщение, если "unpin"="true"
									}, {
                                    "day": "fri",
                                    "createmsg": "09:00:00",
                                    "deactivatemsg": "21:00:00"
									}
								],
                            "manualpollsettings": { #настройки команды, если пользователь запустил ее вручную.
                                "pin": "true", # закрепить сообщение, если pin=true
                                "unpin": "true", # открепить сообщение в время указанное в deactivatemsg, если unpin=true
								"timetolife": 8 # количество часов, которые бот будет отслеживать ответы пользователей на сообщение, которое генерируется данной командой. Также после этого времени бот должен открепить сообщение, если "unpin"="true"
                            },
                            "question": "Кто сегодня на тренировку по рапире?", #Основной текст сообщения, которое генерирует бот по данной команде.
                        }
}
}
}
}
}
Алгоритм работы бота:
1.Бот считывает настройки из файла settings.json.
2.Бот мониторит чат на предмет отправки каким либо пользователем какой либо команды из раздела commands, команды в разделе 
3.Если обнаруживает команду из списка, то он создает сообщение с текстом из ключа "question". Это сообщение получает статус "активное".
4.Пользователи могут добавляться в список и удаляться из него, отправляя символы + (плюс) и - (минус) в чат.
Если пользователь пишет +, то бот добавляет под основным вопросом  порядковый номер, username и имя пользователя. Если пользователь пишет -, то бот удаляет его из списка и заново расставляет порядковые номера пользователей.
5.Если в настройках команды в разделе "manualpollsettings" указано "pin"="true" тогда, бот создает сообщение и закрепляет его в чате.
6.Бот должен следить за созданным вручную (по команде пользователя) сообщением (а именно - обновлять список, если в чате появляются команды + или -)  в течение времени, пока сообщение имеет статус "активное". Для сообщений, созданных вручную это время указанно в settings.json в разделе "manualpollsettings" в ключе "timetolife". Время указано в часах.
7.По истечении времени, указанного в "timetolife" бот перестает обновлять сообщение (не реагирует на символы + или - в чате). Также, если бот закреплял сообщение и в разделе "manualpollsettings" ключ "unpin"= "true" то бот должен открепить сообщение.
8.Помимо ручного вызова команды, команды могут вызываться автоматически по расписанию, если "autopoll"="true". 
9.Настройки команд, вызванных по расписанию находятся в разделе autopollsettings. Там в разделе  schedule_autopoll указаны дни недели (например "day": "thu"), время (например "createmsg": "09:00") - когда бот должен вызвать команду (т.е. самостоятельно создать "активное" сообщение).
10.Автоматически созданное сообщение сохраняет активность до наступления времени, указанного в ключе "deactivatemsg".
11.Настройки pin и unpin для автоматически созданных сообщений берутся из раздела autopollsettings. 
12.ВАЖНО: одновременно может быть только одно "активное" сообщение (бот отслеживает только ОДНО активное сообщение). Поэтому, если в данный момент есть активное сообщение, то бот не может создать еще одно, ни вручную, ни автоматически, т.е. при наличии отслеживаемого сообщения бот игнорирует команды создания сообщений от пользователя и не создает сообщений по расписанию (пропускает).
13.По команде /deactivate бот снять с него статус "активное" (деактивировать его). Под деактивацией понимается: бот перестает реагировать на заявки пользователей в чате + или -, открепляет сообщение, если оно было закреплено и настройка ключа unpin=true. В этом состоянии бот ожидает от пользователей команд ручного вызова (например /saber или /rapier) и также может генерировать активное сообщение самостоятельно, если это разрешено настройкой "autopoll": "true" и настал день и час по расписанию в разделе chedule_autopoll. 

Напиши код такого бота и, приведи в рабочее состояние содержимое файла settings.json






Описание команд типа "type": "poll":
ДАнная команда создает голосование Параметры голосования:
Вопрос: "question": "Сабля сегодня?" - 
Анонимный или открытый опрос: "anonim":"falce",
Отображение текущих результатов: "currentresult": "true"
Варианты ответов: "options": [
                                "Буду 100%",
                                "Пас",
                                "50/50"
                            ]
Дополнительные команды, которые вызываются сразу после данной: "extracommands": []
Подробно про опцию "currentresult": "true": Если true, тогда я хочу, чтобы сразу после формы голосования бот отправил сообщение по шаблону:
1 вариант ответа (2):
пользователь1
пользователь2
2 вариант ответа (0):
3 вариант ответа (1):
пользователь 3

В скобках указать количество человек. Бот должен отслеживать голосование и обновлять свое сообщение под ним в течение количества часов, указанных в параметре "timelife", или 48 часов, если "timelife" не задан.
Если какой то параметр не задан, то, если возможно, и не указано иное, считать его равным false.


должны логироваться в консоль все сообщения полученные ботом и ответы и действия бота.
Вот примерно как должен выглядеть файл настроек settings.json, доработай его до минимальной рабочей версии и пришли мне вместе с новым кодом бота:
{
    "chats": {
        "-1002665411575": { #id чата в который бот будет отправлять сообщения
			"admins": ["84324980"], #id администратора бота
            "topics": { #чат разделен на топики
                "root": { #это топик general
                    "commands": {
                        "somename": { #id команды, эта команда запускается пользователем в чате /saber
							"type": "poll",  #тип команды, для каждого типа должна быть своя обрабатывающая функция в коде бота
							"currentresult": "true"  #флаг наличия дополнительного сообщения от бота под опросом
                            "settings": #настройки опроса, которые предлагает пользователю телеграм (вопрос, варианты ответа, анонимность и т.д.),

Подробно про опцию "currentresult": "true": Если true, тогда я хочу, чтобы сразу после формы голосования бот отправил сообщение по шаблону:
1 вариант ответа (2):
пользователь1
пользователь2
2 вариант ответа (0):
3 вариант ответа (1):
пользователь 3

В скобках указать количество человек. Бот должен отслеживать голосование и обновлять свое сообщение под ним в течение количества часов, указанных в параметре "timelife", или 48 часов, если "timelife" не задан.
Если какой то параметр не задан, то, если возможно, и не указано иное, считать его равным false.
git clone https://github.com/alexander13orlov/votebot2 .

Скачать текущий файл настроек команда getsettings, загрузить файл с настройками: команда setsettings. После загрузки нового файла настроек проверить правильность его синтаксиса, если он с ошибками, то использовать текущий файл, а пользователю отправить сообщение об ошибке. Всегда сохранять на сервере последние три правильных варианта файлов настроек.








Ниже по тексту пример обновленного settings.json, учти я добавил ключ "warnings" для примера только в "autopollsettings", но предупреждение может быть и для 
я добавил ключ предупреждения - "warnings". Такой ключ может быть в ручных и автоматических опросах. 
Внутри этого ключа могут быть одно или несколько предупреждений. На данный момент их два. Первое это "quorum", второе это непогода "badweather".
Предупреждение - это цитирование ботом активного опроса, в цитировании будет размещен текст из ключа "msg".

Предупреждение генерируется по времени и какому либо условию.
Предупрждение quorum срабатывает если за заданный отрезок времени "advancetime" (минуты) до наступления времени "deactivatemsg" (а в опросах, созданных вручную, за время "advancetime" минут до окончания времени "timetolife") количество людей в опросе менее чем указано в ключе "mininum"
Предупреждение badweather срабатывает если за заданный отрезок времени "advancetime" (минуты) до наступления времени "deactivatemsg" (а в опросах, созданных вручную, за время "advancetime" минут до окончания времени "timetolife") вероятность выпадения осадков более чем указано в ключе precipitation по прогнозу на момент времени "deactivatemsg", в диапазоне от момента размещения опроса плюс 12 часов. Если ключ "forecast":"true", то в этом же сообщении разместить почасовой прогноз погоды.


{
    "chats": {
        "-1001570728084": {
            "admins": ["84324980"],
            "topics": {
                "root": {
                    "commands": {
                        "saber": {
                            "autopoll": "true",
                            "autopollsettings": {
                                "warnings": {
                                    "quorum": {
                                        "advancetime": 100,
                                        "mininum": 4,
                                        "msg": "@all, внимание, записалось подозрительно мало людей"
                                    },
                                    "badweather": {
                                        "advancetime": 120,
                                        "precipitation": 60,
                                        "forecast": "true",
                                        "msg": "Сегодня вероятно выпадение осадков"
                                    }
                                },
                                "pin": "true",
                                "unpin": "true",
                                "deleteplusminus": "true",
                                "schedule_autopoll": [{
                                        "day": "tue",
                                        "createmsg": "10:00:00",
                                        "deactivatemsg": "20:15:00"
                                    }, {
                                        "day": "thu",
                                        "createmsg": "10:00:00",
                                        "deactivatemsg": "20:15:00"
                                    }
                                ]
                            },
                            "manualpollsettings": {
                                "pin": "true",
                                "unpin": "true",
                                "deleteplusminus": "true",
                                "timetolife": 480
                            },
                            "question": "Сабля в 20.00?"
                        },
                        "rapier": {
                            "autopoll": "true",
                            "autopollsettings": {
                                "pin": "true",
                                "unpin": "true",
                                "schedule_autopoll": [{
                                        "day": "mon",
                                        "createmsg": "10:00:00",
                                        "deactivatemsg": "20:15:00"
                                    }, {
                                        "day": "fri",
                                        "createmsg": "10:00:00",
                                        "deactivatemsg": "20:15:00"
                                    }
                                ]
                            },
                            "manualpollsettings": {
                                "pin": "true",
                                "unpin": "true",
                                "timetolife": 480
                            },
                            "question": "Рапира в 20.00?"
                        }
                    }
                }
            }
        }
    }
}

Задача: дописать код, чтобы он обрабатывал предупреждение типа quorum. Когда это отладим, можно будет внедрить обработку предупреждений типа badweather, пока вместо badweather ставь заглушку.

Вот текущий рабочий код: 
# -*- coding: utf-8 -*-
import asyncio
import json
import logging
from datetime import datetime, timedelta, time, date, timezone
from dateutil import parser
LOCAL_TZ = timezone(timedelta(hours=3))  # локальный часовой пояс (UTC+3)

from pathlib import Path
from typing import Optional, List, Dict, Any

from aiogram import Bot, Dispatcher, types, F
from aiogram.filters import Command
from aiogram.types import Message
from aiogram.exceptions import TelegramBadRequest

from .config import BOT_TOKEN  # оставлено как есть (config.py в пакете bot)

logging.basicConfig(level=logging.INFO, format='[%(asctime)s] %(levelname)s: %(message)s')
logger = logging.getLogger(__name__)

DATA_DIR = Path(__file__).parent
SETTINGS_PATH = DATA_DIR / "settings.json"
HISTORY_PATH = DATA_DIR / "polls_history.json"  # файл для хранения последних 100 опросов

with open(SETTINGS_PATH, "r", encoding="utf-8") as f:
    SETTINGS = json.load(f)

bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()

# В памяти — активный опрос на каждом чате (поддерживается не больше одного активного опроса глобально)
# active_poll: { chat_id: { "command": str, "message_id": int, "expires_at": datetime, "pinned": bool, "unpin": bool, "participants": [ (uid, username, fullname), ... ] } }
active_poll: Dict[int, Dict[str, Any]] = {}

# Для предотвращения повторного автозапуска одного и того же расписания в один день
last_autocreate: Dict[tuple, date] = {}
last_autodeactivate = {}
# История — список последних опросов (новейшие в начале)
history: List[Dict[str, Any]] = []


def parse_time_str(t: str) -> time:
    h, m, s = [int(x) for x in t.split(":")]
    return time(hour=h, minute=m, second=s)


def user_display_name(user: types.User) -> str:
    if user.username:
        return f"@{user.username} ({user.full_name})"
    return f"{user.full_name}"


def _serialize_participants(participants: List[tuple]) -> List[Dict[str, Any]]:
    return [{"uid": p[0], "username": p[1], "fullname": p[2]} for p in participants]


def _deserialize_participants(data: List[Dict[str, Any]]) -> List[tuple]:
    return [(d["uid"], d.get("username"), d.get("fullname")) for d in data]


def load_history():
    global history, active_poll
    if HISTORY_PATH.exists():
        try:
            with open(HISTORY_PATH, "r", encoding="utf-8") as f:
                history = json.load(f)
            # Найдём активные записи и восстановим только последнюю активную (восстанавливаем только один активный)
            active_entries = [h for h in history if h.get("active")]
            if active_entries:
                # возьмём наиболее позднюю по created_at
                active_entries.sort(key=lambda x: x.get("created_at", ""), reverse=True)
                entry = active_entries[0]
                chat_id = int(entry["chat_id"])
                # Восстановим в active_poll
                active_poll.clear()  # гарантируем, что только одна активная запись
                active_poll[chat_id] = {
                    "command": entry["command"],
                    "message_id": int(entry["message_id"]),
                    "expires_at": datetime.fromisoformat(entry["expires_at"]) if entry.get("expires_at") else None,
                    "pinned": bool(entry.get("pinned", False)),
                    "unpin": bool(entry.get("unpin", False)),
                    "participants": _deserialize_participants(entry.get("participants", [])),
                }
                logger.info("Restored active poll from history: chat=%s message=%s command=%s",
                            chat_id, entry["message_id"], entry["command"])
            else:
                active_poll.clear()
        except Exception as e:
            logger.exception("Failed to load history: %s", e)
            history = []
            active_poll.clear()
    else:
        history = []
        active_poll.clear()


def save_history():
    try:
        with open(HISTORY_PATH, "w", encoding="utf-8") as f:
            json.dump(history[:100], f, ensure_ascii=False, indent=2)
        logger.info("Saved history: %d entries -> %s", len(history[:100]), HISTORY_PATH)
    except Exception as e:
        logger.exception("Failed to save history: %s", e)


def add_history_entry(entry: Dict[str, Any]):
    """
    Добавляет новую запись в историю (в начало списка), держит максимум 100 элементов.
    """
    history.insert(0, entry)
    # Обрезаем до 100
    if len(history) > 100:
        del history[100:]
    save_history()


def update_history_entry(chat_id: int, message_id: int, **updates):
    """
    Находит запись по chat_id и message_id и обновляет её полями updates.
    Если не найдено — логируем предупреждение.
    """
    found = False
    for h in history:
        try:
            if int(h.get("chat_id")) == int(chat_id) and int(h.get("message_id")) == int(message_id):
                h.update(updates)
                found = True
                break
        except Exception:
            # если в данных что-то необычное — пропускаем запись
            continue

    if found:
        save_history()
        logger.info("Updated history entry: chat=%s message=%s updates=%s", chat_id, message_id, list(updates.keys()))
    else:
        logger.warning("History entry not found for update: chat=%s message=%s updates=%s", chat_id, message_id, updates)



async def edit_poll_message(chat_id: int, message_id: int, question: str, participants: List[tuple]):
    total = len(participants)
    lines = [f"[{total}]", question, ""]
    if participants:
        for p in participants:
            uid, username, fullname = p
            if username:
                lines.append(f"@{username}")
            else:
                lines.append(f"{fullname}")
    else:
        lines.append("Пока нет участников.")
    text = "\n".join(lines)
    try:
        await bot.edit_message_text(chat_id=chat_id, message_id=message_id, text=text)
    except TelegramBadRequest as e:
        logger.warning("Failed to edit poll message chat=%s message=%s: %s", chat_id, message_id, e)


def find_command_settings(chat_id: int, command_name: str) -> Optional[dict]:
    chat_conf = SETTINGS["chats"].get(str(chat_id))
    if not chat_conf:
        return None
    topics = chat_conf.get("topics", {})
    topic = topics.get("root", {})
    commands = topic.get("commands", {})
    return commands.get(command_name)


async def create_poll(chat_id: int, command_name: str, *, by_auto=False, schedule_entry: Optional[dict] = None):
    # Если есть активный опрос в любом чате — пропускаем (требование: максимум один активный глобально)
    if active_poll:
        logger.info("There is already an active poll, skipping creation of %s", command_name)
        return None

    cmd_settings = find_command_settings(chat_id, command_name)
    if not cmd_settings:
        logger.info("Command settings not found for %s in chat %s", command_name, chat_id)
        return None

    question = cmd_settings.get("question", f"Опрос: {command_name}")

    pinned = False
    unpin = False
    if by_auto:
        aps = cmd_settings.get("autopollsettings", {})
        pin = aps.get("pin", "false").lower() == "true"
        unpin = aps.get("unpin", "false").lower() == "true"
        deactivatemsg = schedule_entry.get("deactivatemsg")
        deact_time = parse_time_str(deactivatemsg)
        today = date.today()
        expires_at = datetime.combine(today, deact_time)
    else:
        mps = cmd_settings.get("manualpollsettings", {})
        pin = mps.get("pin", "false").lower() == "true"
        unpin = mps.get("unpin", "false").lower() == "true"
        # timetolife хранится в минутaх в config — если это часы прежде, нужно менять в config
        ttl_minutes = int(mps.get("timetolife", 480))
        expires_at = datetime.utcnow() + timedelta(minutes=ttl_minutes)

    # Создаём СОВСЕМ НОВОЕ сообщение (никогда не переиспользуем старое)
    text = f"{question}\n\nПока нет участников."
    sent = await bot.send_message(chat_id, text)
    message_id = sent.message_id

    if pin:
        try:
            await bot.pin_chat_message(chat_id, message_id)
            pinned = True
        except Exception as e:
            logger.warning("Pin failed: %s", e)

    # Запомним активный опрос в памяти
    active_poll.clear()
    active_poll[chat_id] = {
        "command": command_name,
        "message_id": message_id,
        "expires_at": expires_at,
        "pinned": pinned,
        "participants": [],
        "unpin": unpin,
        "created_at": datetime.utcnow().isoformat(),
    }

    # Добавим запись в историю (active=True)
    entry = {
        "chat_id": str(chat_id),
        "message_id": str(message_id),
        "command": command_name,
        "participants": _serialize_participants([]),
        "created_at": active_poll[chat_id]["created_at"],
        "expires_at": expires_at.isoformat() if expires_at else None,
        "active": True,
        "pinned": pinned,
        "unpin": unpin,
    }
    add_history_entry(entry)

    logger.info("Created poll %s in chat %s, message_id=%s expires_at=%s", command_name, chat_id, message_id, expires_at.isoformat())
    return active_poll[chat_id]


async def deactivate_poll(chat_id: int, reason="manual"):
    info = active_poll.get(chat_id)
    if not info:
        logger.info("No active poll in chat %s to deactivate", chat_id)
        return False

    message_id = info["message_id"]
    pinned = info.get("pinned", False)
    unpin = info.get("unpin", False)

    unpin_success = False
    # Попытка открепить, если это нужно
    if pinned and unpin:
        try:
            await bot.unpin_chat_message(chat_id=str(chat_id), message_id=message_id)
            unpin_success = True
            info["pinned"] = False
            logger.info("Successfully unpinned message %s in chat %s", message_id, chat_id)
        except Exception as e:
            logger.warning("Unpin failed: %s", e)

    # Построим итоговый текст с пометкой "ЗАКРЫТ"
    question = find_command_settings(chat_id, info["command"]).get("question", "Опрос завершён")
    participants = info.get("participants", [])
    total = len(participants)
    lines = [f"[{total}] {question} (ЗАКРЫТ)", ""]
    if participants:
        for idx, p in enumerate(participants, start=1):
            uid, username, fullname = p
            if username:
                lines.append(f"{idx}. @{username} — {fullname}")
            else:
                lines.append(f"{idx}. {fullname}")
    else:
        lines.append("Никто не записался.")

    # Пытаемся обновить текст сообщения (именованные аргументы + chat_id как строка)
    try:
        await bot.edit_message_text(text="\n".join(lines), chat_id=str(chat_id), message_id=message_id)
        edit_ok = True
    except Exception as e:
        edit_ok = False
        logger.warning("Failed to edit message when closing poll chat=%s message=%s: %s", chat_id, message_id, e)

    # Решаем финальное значение pinned в истории: если we successfully unpinned -> False, иначе сохраняем текущее info['pinned']
    pinned_value = False if unpin_success else bool(info.get("pinned", False))

    # Обновим историю: пометим запись как inactive и установим pinned_value и финальный список участников
    update_history_entry(chat_id, message_id,
                         active=False,
                         pinned=pinned_value,
                         participants=_serialize_participants(participants))

    logger.info("History updated for chat=%s message=%s active=False pinned=%s edit_ok=%s",
                chat_id, message_id, pinned_value, edit_ok)

    # Удалим активный опрос из памяти — после деактивации он не должен более использоваться
    try:
        del active_poll[chat_id]
    except KeyError:
        pass

    logger.info("Deactivated poll in %s (%s). unpin_success=%s pinned_value=%s", chat_id, reason, unpin_success, pinned_value)
    return True

# --- Handlers --- #

@dp.message(Command(commands=["saber"]))
async def saber_cmd(message: Message):
    chat_id = message.chat.id
    await create_poll(chat_id, "saber")


@dp.message(Command(commands=["rapier"]))
async def rapier_cmd(message: Message):
    chat_id = message.chat.id
    await create_poll(chat_id, "rapier")


@dp.message(Command(commands=["deactivate"]))
async def deactivate_cmd(message: Message):
    chat_id = message.chat.id
    res = await deactivate_poll(chat_id, reason=f"manual by {message.from_user.id}")
    if res:
        await message.reply("Опрос деактивирован.")
    else:
        await message.reply("Активных опросов нет.")


@dp.message(F.text.in_({"+", "-"}))
async def plus_minus_handler(message: Message):
    chat_id = message.chat.id
    text = message.text.strip()
    info = active_poll.get(chat_id)
    if not info:
        return

    # проверяем expiry
    if info.get("expires_at") and datetime.utcnow() >= info["expires_at"]:
        await deactivate_poll(chat_id, reason="expired")
        return

    uid = message.from_user.id
    username = message.from_user.username
    fullname = message.from_user.full_name
    participants = info["participants"]

    cmd_settings = find_command_settings(chat_id, info["command"])
    delete_pm = False

    # Проверяем deleteplusminus в зависимости от типа опроса
    if info in active_poll.values():
        if "autopollsettings" in cmd_settings and info.get("expires_at"):
            delete_pm = cmd_settings.get("autopollsettings", {}).get("deleteplusminus", "false").lower() == "true"
        elif "manualpollsettings" in cmd_settings:
            delete_pm = cmd_settings.get("manualpollsettings", {}).get("deleteplusminus", "false").lower() == "true"

    changed = False
    if text == "+":
        if not any(p[0] == uid for p in participants):
            participants.append((uid, username, fullname))
            changed = True
    elif text == "-":
        if any(p[0] == uid for p in participants):
            participants[:] = [p for p in participants if p[0] != uid]
            changed = True

    if changed:
        await edit_poll_message(chat_id, info["message_id"], cmd_settings["question"], participants)
        update_history_entry(chat_id, info["message_id"], participants=_serialize_participants(participants))

        if delete_pm:
            await asyncio.sleep(15) #удаляем из чата пользовательские + и - 
            try:
                await message.delete()
            except Exception:
                pass



async def autopoll_scheduler():
    logger.info("Autopoll scheduler started")
    while True:
        try:
            now_utc = datetime.utcnow().replace(tzinfo=timezone.utc)
            now_local = now_utc.astimezone(LOCAL_TZ)
            logger.debug(f"[autopoll] Tick at {now_local.isoformat()} (weekday={now_local.strftime('%a').lower()[:3]})")

            # Проверка и деактивация активных опросов
            for cid, info in list(active_poll.items()):
                expires_at = info.get("expires_at")
                if expires_at:
                    # Приводим expires_at к timezone-aware, если нужно
                    if expires_at.tzinfo is None:
                        expires_at = expires_at.replace(tzinfo=LOCAL_TZ)
                    key_deact = (cid,)
                    already_deact = last_autodeactivate.get(key_deact)
                    if now_local >= expires_at and already_deact != date.today():
                        logger.info(f"[autopoll] Deactivating poll {cid} due to expiration")
                        await deactivate_poll(cid, reason="expired by scheduler")
                        last_autodeactivate[key_deact] = date.today()

            # Если уже есть активный опрос, пропускаем создание новых
            if active_poll:
                await asyncio.sleep(30)
                continue

            # Проходим по чатам и командам
            for chat_id_str, chat_conf in SETTINGS["chats"].items():
                chat_id = int(chat_id_str)
                topics = chat_conf.get("topics", {})
                topic = topics.get("root", {})
                commands = topic.get("commands", {})

                for cmd_name, cmd_conf in commands.items():
                    if cmd_conf.get("autopoll", "false").lower() != "true":
                        continue

                    aps = cmd_conf.get("autopollsettings", {})
                    schedule_list = aps.get("schedule_autopoll", [])

                    for sched in schedule_list:
                        day = sched.get("day", "").strip().lower()[:3]
                        create_time = parse_time_str(sched.get("createmsg"))
                        today_weekday = now_local.strftime("%a").lower()[:3]

                        if day != today_weekday:
                            continue

                        sched_dt = datetime.combine(date.today(), create_time).replace(tzinfo=LOCAL_TZ)
                        key = (chat_id, cmd_name, day, sched.get("createmsg"))
                        already = last_autocreate.get(key)

                        logger.debug(
                            f"[autopoll] Check schedule: cmd={cmd_name}, day={day}, target={sched.get('createmsg')}, "
                            f"today={today_weekday}, sched_dt={sched_dt.isoformat()}, now_local={now_local.isoformat()}"
                        )

                        # Проверяем окно запуска (60 секунд)
                        if sched_dt <= now_local < (sched_dt + timedelta(seconds=60)):
                            if already == date.today():
                                logger.debug(f"[autopoll] Already executed today for {cmd_name}")
                                continue
                            if active_poll:
                                logger.debug(f"[autopoll] Active poll exists, skip creating new {cmd_name}")
                                last_autocreate[key] = date.today()
                                continue

                            logger.info(f"[autopoll] Triggering scheduled autopoll for {cmd_name} (chat {chat_id})")
                            await create_poll(chat_id, cmd_name, by_auto=True, schedule_entry=sched)
                            last_autocreate[key] = date.today()

        except Exception as e:
            logger.exception("Error in autopoll scheduler: %s", e)

        await asyncio.sleep(30)

def build_help_text():
    lines = [
        "🤖 *Бот для управления опросами*\n",
        "*Основные команды:*"
    ]

    for chat_id_str, chat_conf in SETTINGS.get("chats", {}).items():
        lines.append(f"\n*Чат:* `{chat_id_str}`")
        topics = chat_conf.get("topics", {})
        topic = topics.get("root", {})
        commands = topic.get("commands", {})

        for cmd_name, cmd_conf in commands.items():
            question = cmd_conf.get("question", cmd_name)
            lines.append(f"/{cmd_name} - Создать опрос: \"{question}\"")

            # Автопрос
            if cmd_conf.get("autopoll", "false").lower() == "true":
                lines.append(f"   - Автопрос включён")
                aps = cmd_conf.get("autopollsettings", {})
                schedule_list = aps.get("schedule_autopoll", [])
                for sched in schedule_list:
                    day = sched.get("day", "").capitalize()
                    create_time = sched.get("createmsg")
                    deactivate_time = sched.get("deactivatemsg")
                    lines.append(f"     • {day}: создаётся в {create_time}, закрывается в {deactivate_time}")

            # Настройки ручного опроса
            mps = cmd_conf.get("manualpollsettings", {})
            ttl = mps.get("timetolife", 480)
            pin = mps.get("pin", "false").lower() == "true"
            unpin = mps.get("unpin", "false").lower() == "true"
            lines.append(f"   - Время жизни ручного опроса: {ttl} мин. Pin: {pin}, Unpin: {unpin}")

    lines.append("\n*Участие в опросе:*")
    lines.append("- Чтобы записаться, отправьте `+`")
    lines.append("- Чтобы снять участие, отправьте `-`")
    lines.append("\n*Деактивация опроса:*")
    lines.append("- /deactivate - закрыть активный опрос в этом чате")
    lines.append("\n*История:*")
    lines.append("- Бот хранит последние 100 опросов и восстанавливает активный при перезапуске")
    return "\n".join(lines)

@dp.message(Command(commands=["help"]))
async def help_cmd(message: types.Message):
    text = build_help_text()
    sent = await message.answer(text, parse_mode="Markdown")
    await asyncio.sleep(100)
    try:
        await sent.delete()
    except Exception:
        pass


async def main():
    # Загрузим историю и восстановим (если есть) активный опрос
    load_history()
    # Запустим автопланировщик
    asyncio.create_task(autopoll_scheduler())
    # Запуск Polling
    await dp.start_polling(bot)


if __name__ == "__main__":
    asyncio.run(main())










Вот текущая структура settings.json
{
  "chats": {
    "-1002665411575": {
      "admins": ["84324980"],
      "topics": {
        "root": {
          "commands": {
            "saber": {
              "autopoll": "true",
              "autopollsettings": {
                "pin": "true",
                "unpin": "true",
                "schedule_autopoll": [
                  {
                    "day": "tue",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "20:15:00"
                  },
                  {
                    "day": "thu",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "20:15:00"
                  }
                ]
              },
              "manualpollsettings": {
                "pin": "true",
                "unpin": "true",
                "timetolife": 480
              },
              "question": "Сабля в 20.00?"
            },
            "rapier": {
              "autopoll": "true",
              "autopollsettings": {
                "pin": "true",
                "unpin": "true",
                "schedule_autopoll": [
                  {
                    "day": "mon",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "20:15:00"
                  },
                  {
                    "day": "fri",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "20:15:00"
                  }
                ]
              },
              "manualpollsettings": {
                "pin": "true",
                "unpin": "true",
                "timetolife": 480
              },
              "question": "Рапира в 20.00?"
            }
          }
        }
      }
    }
  }
}

В ней разделены настройки "autopollsettings" и "manualpollsettings", я хочу упростить, убрать ключ "manualpollsettings", а ключ "autopollsettings" переименовать в "pollsettings". Т.е. сделать так, чтобы настройки запросов, которые бот запускает по расписанию были такие же, как и в опросах, которые пользователь запускает вручную. Вместо "timetolife", которые есть в ручных опросах исползовать "deactivatemsg". Логика деакцивации должна быть такая: от момента создания ручного опроса бот ищет в настройках этого опроса ближайший день недели и время его деактивации "deactivatemsg"
Вот текущй рабочий код, там используется динамическая справка, ее тоже надо будет обновить:
# -*- coding: utf-8 -*-
import asyncio
import json
import logging
from datetime import datetime, timedelta, time, date, timezone
from dateutil import parser
LOCAL_TZ = timezone(timedelta(hours=3))  # локальный часовой пояс (UTC+3)

from pathlib import Path
from typing import Optional, List, Dict, Any

from aiogram import Bot, Dispatcher, types, F
from aiogram.filters import Command
from aiogram.types import Message
from aiogram.exceptions import TelegramBadRequest

from .config import BOT_TOKEN  # оставлено как есть (config.py в пакете bot)

logging.basicConfig(level=logging.INFO, format='[%(asctime)s] %(levelname)s: %(message)s')
logger = logging.getLogger(__name__)

DATA_DIR = Path(__file__).parent
SETTINGS_PATH = DATA_DIR / "settings.json"
HISTORY_PATH = DATA_DIR / "polls_history.json"  # файл для хранения последних 100 опросов

with open(SETTINGS_PATH, "r", encoding="utf-8") as f:
    SETTINGS = json.load(f)

bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()

# В памяти — активный опрос на каждом чате (поддерживается не больше одного активного опроса глобально)
# active_poll: { chat_id: { "command": str, "message_id": int, "expires_at": datetime, "pinned": bool, "unpin": bool, "participants": [ (uid, username, fullname), ... ] } }
active_poll: Dict[int, Dict[str, Any]] = {}

# Для предотвращения повторного автозапуска одного и того же расписания в один день
last_autocreate: Dict[tuple, date] = {}
last_autodeactivate = {}
# История — список последних опросов (новейшие в начале)
history: List[Dict[str, Any]] = []


def parse_time_str(t: str) -> time:
    h, m, s = [int(x) for x in t.split(":")]
    return time(hour=h, minute=m, second=s)


def user_display_name(user: types.User) -> str:
    if user.username:
        return f"@{user.username} ({user.full_name})"
    return f"{user.full_name}"


def _serialize_participants(participants: List[tuple]) -> List[Dict[str, Any]]:
    return [{"uid": p[0], "username": p[1], "fullname": p[2]} for p in participants]


def _deserialize_participants(data: List[Dict[str, Any]]) -> List[tuple]:
    return [(d["uid"], d.get("username"), d.get("fullname")) for d in data]


def load_history():
    global history, active_poll
    if HISTORY_PATH.exists():
        try:
            with open(HISTORY_PATH, "r", encoding="utf-8") as f:
                history = json.load(f)
            # Найдём активные записи и восстановим только последнюю активную (восстанавливаем только один активный)
            active_entries = [h for h in history if h.get("active")]
            if active_entries:
                # возьмём наиболее позднюю по created_at
                active_entries.sort(key=lambda x: x.get("created_at", ""), reverse=True)
                entry = active_entries[0]
                chat_id = int(entry["chat_id"])
                # Восстановим в active_poll
                active_poll.clear()  # гарантируем, что только одна активная запись
                active_poll[chat_id] = {
                    "command": entry["command"],
                    "message_id": int(entry["message_id"]),
                    "expires_at": datetime.fromisoformat(entry["expires_at"]) if entry.get("expires_at") else None,
                    "pinned": bool(entry.get("pinned", False)),
                    "unpin": bool(entry.get("unpin", False)),
                    "participants": _deserialize_participants(entry.get("participants", [])),
                }
                logger.info("Restored active poll from history: chat=%s message=%s command=%s",
                            chat_id, entry["message_id"], entry["command"])
            else:
                active_poll.clear()
        except Exception as e:
            logger.exception("Failed to load history: %s", e)
            history = []
            active_poll.clear()
    else:
        history = []
        active_poll.clear()


def save_history():
    try:
        with open(HISTORY_PATH, "w", encoding="utf-8") as f:
            json.dump(history[:100], f, ensure_ascii=False, indent=2)
        logger.info("Saved history: %d entries -> %s", len(history[:100]), HISTORY_PATH)
    except Exception as e:
        logger.exception("Failed to save history: %s", e)


def add_history_entry(entry: Dict[str, Any]):
    """
    Добавляет новую запись в историю (в начало списка), держит максимум 100 элементов.
    """
    history.insert(0, entry)
    # Обрезаем до 100
    if len(history) > 100:
        del history[100:]
    save_history()


def update_history_entry(chat_id: int, message_id: int, **updates):
    """
    Находит запись по chat_id и message_id и обновляет её полями updates.
    Если не найдено — логируем предупреждение.
    """
    found = False
    for h in history:
        try:
            if int(h.get("chat_id")) == int(chat_id) and int(h.get("message_id")) == int(message_id):
                h.update(updates)
                found = True
                break
        except Exception:
            # если в данных что-то необычное — пропускаем запись
            continue

    if found:
        save_history()
        logger.info("Updated history entry: chat=%s message=%s updates=%s", chat_id, message_id, list(updates.keys()))
    else:
        logger.warning("History entry not found for update: chat=%s message=%s updates=%s", chat_id, message_id, updates)



async def edit_poll_message(chat_id: int, message_id: int, question: str, participants: List[tuple]):
    total = len(participants)
    lines = [f"[{total}]", question, ""]
    if participants:
        for p in participants:
            uid, username, fullname = p
            if username:
                lines.append(f"@{username}")
            else:
                lines.append(f"{fullname}")
    else:
        lines.append("Пока нет участников.")
    text = "\n".join(lines)
    try:
        await bot.edit_message_text(chat_id=chat_id, message_id=message_id, text=text)
    except TelegramBadRequest as e:
        logger.warning("Failed to edit poll message chat=%s message=%s: %s", chat_id, message_id, e)


def find_command_settings(chat_id: int, command_name: str) -> Optional[dict]:
    chat_conf = SETTINGS["chats"].get(str(chat_id))
    if not chat_conf:
        return None
    topics = chat_conf.get("topics", {})
    topic = topics.get("root", {})
    commands = topic.get("commands", {})
    return commands.get(command_name)


async def create_poll(chat_id: int, command_name: str, *, by_auto=False, schedule_entry: Optional[dict] = None):
    # Если есть активный опрос в любом чате — пропускаем (требование: максимум один активный глобально)
    if active_poll:
        logger.info("There is already an active poll, skipping creation of %s", command_name)
        return None

    cmd_settings = find_command_settings(chat_id, command_name)
    if not cmd_settings:
        logger.info("Command settings not found for %s in chat %s", command_name, chat_id)
        return None

    question = cmd_settings.get("question", f"Опрос: {command_name}")

    pinned = False
    unpin = False
    if by_auto:
        aps = cmd_settings.get("autopollsettings", {})
        pin = aps.get("pin", "false").lower() == "true"
        unpin = aps.get("unpin", "false").lower() == "true"
        deactivatemsg = schedule_entry.get("deactivatemsg")
        deact_time = parse_time_str(deactivatemsg)
        today = date.today()
        expires_at = datetime.combine(today, deact_time)
    else:
        mps = cmd_settings.get("manualpollsettings", {})
        pin = mps.get("pin", "false").lower() == "true"
        unpin = mps.get("unpin", "false").lower() == "true"
        # timetolife хранится в минутaх в config — если это часы прежде, нужно менять в config
        ttl_minutes = int(mps.get("timetolife", 480))
        expires_at = datetime.utcnow() + timedelta(minutes=ttl_minutes)

    # Создаём СОВСЕМ НОВОЕ сообщение (никогда не переиспользуем старое)
    text = f"{question}\n\nПока нет участников."
    sent = await bot.send_message(chat_id, text)
    message_id = sent.message_id

    if pin:
        try:
            await bot.pin_chat_message(chat_id, message_id)
            pinned = True
        except Exception as e:
            logger.warning("Pin failed: %s", e)

    # Запомним активный опрос в памяти
    active_poll.clear()
    active_poll[chat_id] = {
        "command": command_name,
        "message_id": message_id,
        "expires_at": expires_at,
        "pinned": pinned,
        "participants": [],
        "unpin": unpin,
        "created_at": datetime.utcnow().isoformat(),
    }

    # Добавим запись в историю (active=True)
    entry = {
        "chat_id": str(chat_id),
        "message_id": str(message_id),
        "command": command_name,
        "participants": _serialize_participants([]),
        "created_at": active_poll[chat_id]["created_at"],
        "expires_at": expires_at.isoformat() if expires_at else None,
        "active": True,
        "pinned": pinned,
        "unpin": unpin,
    }
    add_history_entry(entry)

    logger.info("Created poll %s in chat %s, message_id=%s expires_at=%s", command_name, chat_id, message_id, expires_at.isoformat())
    return active_poll[chat_id]


async def deactivate_poll(chat_id: int, reason="manual"):
    info = active_poll.get(chat_id)
    if not info:
        logger.info("No active poll in chat %s to deactivate", chat_id)
        return False

    message_id = info["message_id"]
    pinned = info.get("pinned", False)
    unpin = info.get("unpin", False)

    unpin_success = False
    # Попытка открепить, если это нужно
    if pinned and unpin:
        try:
            await bot.unpin_chat_message(chat_id=str(chat_id), message_id=message_id)
            unpin_success = True
            info["pinned"] = False
            logger.info("Successfully unpinned message %s in chat %s", message_id, chat_id)
        except Exception as e:
            logger.warning("Unpin failed: %s", e)

    # Построим итоговый текст с пометкой "ЗАКРЫТ"
    question = find_command_settings(chat_id, info["command"]).get("question", "Опрос завершён")
    participants = info.get("participants", [])
    total = len(participants)
    lines = [f"[{total}] {question} (ЗАКРЫТ)", ""]
    if participants:
        for idx, p in enumerate(participants, start=1):
            uid, username, fullname = p
            if username:
                lines.append(f"{idx}. @{username} — {fullname}")
            else:
                lines.append(f"{idx}. {fullname}")
    else:
        lines.append("Никто не записался.")

    # Пытаемся обновить текст сообщения (именованные аргументы + chat_id как строка)
    try:
        await bot.edit_message_text(text="\n".join(lines), chat_id=str(chat_id), message_id=message_id)
        edit_ok = True
    except Exception as e:
        edit_ok = False
        logger.warning("Failed to edit message when closing poll chat=%s message=%s: %s", chat_id, message_id, e)

    # Решаем финальное значение pinned в истории: если we successfully unpinned -> False, иначе сохраняем текущее info['pinned']
    pinned_value = False if unpin_success else bool(info.get("pinned", False))

    # Обновим историю: пометим запись как inactive и установим pinned_value и финальный список участников
    update_history_entry(chat_id, message_id,
                         active=False,
                         pinned=pinned_value,
                         participants=_serialize_participants(participants))

    logger.info("History updated for chat=%s message=%s active=False pinned=%s edit_ok=%s",
                chat_id, message_id, pinned_value, edit_ok)

    # Удалим активный опрос из памяти — после деактивации он не должен более использоваться
    try:
        del active_poll[chat_id]
    except KeyError:
        pass

    logger.info("Deactivated poll in %s (%s). unpin_success=%s pinned_value=%s", chat_id, reason, unpin_success, pinned_value)
    return True

# --- Handlers --- #

@dp.message(Command(commands=["saber"]))
async def saber_cmd(message: Message):
    chat_id = message.chat.id
    await create_poll(chat_id, "saber")


@dp.message(Command(commands=["rapier"]))
async def rapier_cmd(message: Message):
    chat_id = message.chat.id
    await create_poll(chat_id, "rapier")


@dp.message(Command(commands=["deactivate"]))
async def deactivate_cmd(message: Message):
    chat_id = message.chat.id
    res = await deactivate_poll(chat_id, reason=f"manual by {message.from_user.id}")
    if res:
        await message.reply("Опрос деактивирован.")
    else:
        await message.reply("Активных опросов нет.")


@dp.message(F.text.in_({"+", "-"}))
async def plus_minus_handler(message: Message):
    chat_id = message.chat.id
    text = message.text.strip()
    info = active_poll.get(chat_id)
    if not info:
        return

    # проверяем expiry
    if info.get("expires_at") and datetime.utcnow() >= info["expires_at"]:
        await deactivate_poll(chat_id, reason="expired")
        return

    uid = message.from_user.id
    username = message.from_user.username
    fullname = message.from_user.full_name
    participants = info["participants"]

    cmd_settings = find_command_settings(chat_id, info["command"])
    delete_pm = False

    # Проверяем deleteplusminus в зависимости от типа опроса
    if info in active_poll.values():
        if "autopollsettings" in cmd_settings and info.get("expires_at"):
            delete_pm = cmd_settings.get("autopollsettings", {}).get("deleteplusminus", "false").lower() == "true"
        elif "manualpollsettings" in cmd_settings:
            delete_pm = cmd_settings.get("manualpollsettings", {}).get("deleteplusminus", "false").lower() == "true"

    changed = False
    if text == "+":
        if not any(p[0] == uid for p in participants):
            participants.append((uid, username, fullname))
            changed = True
    elif text == "-":
        if any(p[0] == uid for p in participants):
            participants[:] = [p for p in participants if p[0] != uid]
            changed = True

    if changed:
        await edit_poll_message(chat_id, info["message_id"], cmd_settings["question"], participants)
        update_history_entry(chat_id, info["message_id"], participants=_serialize_participants(participants))

        if delete_pm:
            await asyncio.sleep(15) #удаляем из чата пользовательские + и - 
            try:
                await message.delete()
            except Exception:
                pass



async def autopoll_scheduler():
    logger.info("Autopoll scheduler started")
    while True:
        try:
            now_utc = datetime.utcnow().replace(tzinfo=timezone.utc)
            now_local = now_utc.astimezone(LOCAL_TZ)
            logger.debug(f"[autopoll] Tick at {now_local.isoformat()} (weekday={now_local.strftime('%a').lower()[:3]})")

            # Проверка и деактивация активных опросов
            for cid, info in list(active_poll.items()):
                expires_at = info.get("expires_at")
                if expires_at:
                    # Приводим expires_at к timezone-aware, если нужно
                    if expires_at.tzinfo is None:
                        expires_at = expires_at.replace(tzinfo=LOCAL_TZ)
                    key_deact = (cid,)
                    already_deact = last_autodeactivate.get(key_deact)
                    if now_local >= expires_at and already_deact != date.today():
                        logger.info(f"[autopoll] Deactivating poll {cid} due to expiration")
                        await deactivate_poll(cid, reason="expired by scheduler")
                        last_autodeactivate[key_deact] = date.today()

            # Если уже есть активный опрос, пропускаем создание новых
            if active_poll:
                await asyncio.sleep(30)
                continue

            # Проходим по чатам и командам
            for chat_id_str, chat_conf in SETTINGS["chats"].items():
                chat_id = int(chat_id_str)
                topics = chat_conf.get("topics", {})
                topic = topics.get("root", {})
                commands = topic.get("commands", {})

                for cmd_name, cmd_conf in commands.items():
                    if cmd_conf.get("autopoll", "false").lower() != "true":
                        continue

                    aps = cmd_conf.get("autopollsettings", {})
                    schedule_list = aps.get("schedule_autopoll", [])

                    for sched in schedule_list:
                        day = sched.get("day", "").strip().lower()[:3]
                        create_time = parse_time_str(sched.get("createmsg"))
                        today_weekday = now_local.strftime("%a").lower()[:3]

                        if day != today_weekday:
                            continue

                        sched_dt = datetime.combine(date.today(), create_time).replace(tzinfo=LOCAL_TZ)
                        key = (chat_id, cmd_name, day, sched.get("createmsg"))
                        already = last_autocreate.get(key)

                        logger.debug(
                            f"[autopoll] Check schedule: cmd={cmd_name}, day={day}, target={sched.get('createmsg')}, "
                            f"today={today_weekday}, sched_dt={sched_dt.isoformat()}, now_local={now_local.isoformat()}"
                        )

                        # Проверяем окно запуска (60 секунд)
                        if sched_dt <= now_local < (sched_dt + timedelta(seconds=60)):
                            if already == date.today():
                                logger.debug(f"[autopoll] Already executed today for {cmd_name}")
                                continue
                            if active_poll:
                                logger.debug(f"[autopoll] Active poll exists, skip creating new {cmd_name}")
                                last_autocreate[key] = date.today()
                                continue

                            logger.info(f"[autopoll] Triggering scheduled autopoll for {cmd_name} (chat {chat_id})")
                            await create_poll(chat_id, cmd_name, by_auto=True, schedule_entry=sched)
                            last_autocreate[key] = date.today()

        except Exception as e:
            logger.exception("Error in autopoll scheduler: %s", e)

        await asyncio.sleep(30)

def build_help_text():
    lines = [
        "🤖 *Бот для управления опросами*\n",
        "*Основные команды:*"
    ]

    for chat_id_str, chat_conf in SETTINGS.get("chats", {}).items():
        lines.append(f"\n*Чат:* `{chat_id_str}`")
        topics = chat_conf.get("topics", {})
        topic = topics.get("root", {})
        commands = topic.get("commands", {})

        for cmd_name, cmd_conf in commands.items():
            question = cmd_conf.get("question", cmd_name)
            lines.append(f"/{cmd_name} - Создать опрос: \"{question}\"")

            # Автопрос
            if cmd_conf.get("autopoll", "false").lower() == "true":
                lines.append(f"   - Автопрос включён")
                aps = cmd_conf.get("autopollsettings", {})
                schedule_list = aps.get("schedule_autopoll", [])
                for sched in schedule_list:
                    day = sched.get("day", "").capitalize()
                    create_time = sched.get("createmsg")
                    deactivate_time = sched.get("deactivatemsg")
                    lines.append(f"     • {day}: создаётся в {create_time}, закрывается в {deactivate_time}")

            # Настройки ручного опроса
            mps = cmd_conf.get("manualpollsettings", {})
            ttl = mps.get("timetolife", 480)
            pin = mps.get("pin", "false").lower() == "true"
            unpin = mps.get("unpin", "false").lower() == "true"
            lines.append(f"   - Время жизни ручного опроса: {ttl} мин. Pin: {pin}, Unpin: {unpin}")

    lines.append("\n*Участие в опросе:*")
    lines.append("- Чтобы записаться, отправьте `+`")
    lines.append("- Чтобы снять участие, отправьте `-`")
    lines.append("\n*Деактивация опроса:*")
    lines.append("- /deactivate - закрыть активный опрос в этом чате")
    lines.append("\n*История:*")
    lines.append("- Бот хранит последние 100 опросов и восстанавливает активный при перезапуске")
    return "\n".join(lines)

@dp.message(Command(commands=["help"]))
async def help_cmd(message: types.Message):
    text = build_help_text()
    sent = await message.answer(text, parse_mode="Markdown")
    await asyncio.sleep(100)
    try:
        await sent.delete()
    except Exception:
        pass


async def main():
    # Загрузим историю и восстановим (если есть) активный опрос
    load_history()
    # Запустим автопланировщик
    asyncio.create_task(autopoll_scheduler())
    # Запуск Polling
    await dp.start_polling(bot)


if __name__ == "__main__":
    asyncio.run(main())









"warnings": {"quorum":{"advancetime":100, "mininum":4, "msg":"@all, внимание, записалось подозрительно мало людей"}, "forecast":{"advancetime":120, "rain":60, "msg":"@all, вероятно выпадение осадков"}},







нет, надо чтобы команды работали только внутри своих тем (топиков) например есть топики root, топик с id41 и топик с id53, тогда settings.json будет примерно такой структуры:   
"chats": {
    "-1002665411575": {
      "admins": ["84324980"],
      "topics": {
        "root": {
          "commands": {....}...
		},
		"41": {
			  "commands": {....}...
			},
		"53": {
          "commands": {....}...
		},...
		
В текущий момент в моем settings.json только одна тема root. Я хочу чтобы если пользователь отправил команду в другом топике, то она бы игнорировалась




сейчас согласно функции 
def build_poll_text_with_timer(question: str, participants: List[tuple], expires_at: datetime) -> str:
    """
    Формирует текст опроса с количеством участников и оставшимся временем до закрытия.
    """
    total = len(participants)
    now_utc = datetime.now(timezone.utc)
    remaining = expires_at - now_utc

    if remaining.total_seconds() <= 0:
        remaining_str = "0ч0м"
    else:
        hours, remainder = divmod(int(remaining.total_seconds()), 3600)
        minutes, _ = divmod(remainder, 60)
        remaining_str = f"{hours}ч{minutes}м"

    lines = [f"[{total}] {question}", f"Осталось {remaining_str}.", ""]

    if participants:
        for idx, p in enumerate(participants, start=1):
            uid, username, fullname = p
            if username:
                lines.append(f"{idx}.{fullname} @{username}")
            else:
                lines.append(f"{idx}. {fullname}")
    else:
        lines.append("Пока нет участников.")

    return "\n".join(lines)

согласно строке кода  lines = [f"[{total}] {question}", f"Осталось {remaining_str}.", ""]
заголовок сообщения может выглядеть так: [2]Рапира в 20:00? Осталось ...
Я изменю в settings "Рапира в 20:00?" на "Рапира" и добавлю тег "workoutstart": "20:00:00" и хочу чтобы строка выгляделя так:
[2] Рапира 14 октября 20:00. Осталось ....
Т.е. скрипт должен рассчитать какое будет число и месяц и из тега workoutstart вытащить часы и минуты.
вот пример будущего settings:
{
  "chats": {
    "-1002655543164": {
      "admins": ["84324980"],
      "topics": {
        "root": {
          "commands": {
            "saber": {
              "autopoll": "true",
              "autopollsettings": {
                "pin": "true",
                "unpin": "true",
                "deleteplusminus": "true",
                "schedule_autopoll": [
                  {
                    "day": "tue",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "19:00:00",
                    "workoutstart": "20:00:00"
                  },
                  {
                    "day": "thu",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "19:00:00",
                    "workoutstart": "20:00:00"
                  }
                ]
              },
              "manualpollsettings": {
                "pin": "true",
                "unpin": "true",
                "deleteplusminus": "true"
                
              },
              "question": "Сабля"
            },
            "rapier": {
              "autopoll": "true",
              "autopollsettings": {
                "pin": "true",
                "unpin": "true",
                "deleteplusminus": "true",
                "schedule_autopoll": [
                  {
                    "day": "mon",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "19:00:00",
                    "workoutstart": "20:00:00"
                  },
                  {
                    "day": "fri",
                    "createmsg": "10:00:00",
                    "deactivatemsg": "19:00:00",
                    "workoutstart": "20:00:00"
                  }
                ]
              },
              "manualpollsettings": {
                "pin": "true",
                "unpin": "true",
                "deleteplusminus": "true"
                
              },
              "question": "Рапира"
            },
             "openfight": {
              "autopoll": "true",
              "autopollsettings": {
                "pin": "true",
                "unpin": "true",
                "deleteplusminus": "true",
                "schedule_autopoll": [
                   {
                    "day": "sun",
                    "createmsg": "09:00:00",
                    "deactivatemsg": "13:00:00",
                    "workoutstart": "14:00:00"
                  }
                ]
              },
              "manualpollsettings": {
                "pin": "true",
                "unpin": "true",
                "deleteplusminus": "true"
                
              },
              "question": "Практика"
            }
          }
        }
      }
    }
  }
}







